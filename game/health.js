
class Health {
// extends Item {
    constructor(x, y, width, height, items) {
       // super(x, y, width, height, items);
     

        // super.update();
        // super.pickedUp();
        // super.shrinkAndRemove();
        // super.animate();

    }

    render(){
        fill(245, 98, 186);
        rect(this.x, this.y,this.width, this.height);
    }
    
}